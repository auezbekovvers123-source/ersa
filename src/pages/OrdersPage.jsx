export default function OrdersPage() {
  return (
    <section>
      <h2>Orders</h2>
      <p>
        Person 3: CRUD <code>/orders</code>. Use <code>useAuth().user.id</code>{' '}
        as default <code>userId</code> on create when logged in.
      </p>
    </section>
  )
}
