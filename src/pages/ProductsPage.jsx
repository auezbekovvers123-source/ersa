export default function ProductsPage() {
  return (
    <section>
      <h2>Products</h2>
      <p>
        Person 3: list products (<code>GET /products</code>), link to{' '}
        <code>/products/:id</code>, add create/edit/delete forms. Use{' '}
        <code>apiJson</code> from <code>src/api.js</code> and a category dropdown
        from <code>GET /categories</code>.
      </p>
    </section>
  )
}
